import {API} from '../api/api.js';import {router} from '../core/router.js';import {appState} from '../core/state.js';import {storage} from '../core/storage.js';import {toast} from '../ui/toast.js';
export function joinRoomPage(){return `<div class="shell page"><div class="topbar"><div class="brand">GEO<span>BATTLE</span></div><button class="btn ghost" onclick="history.back()">← Voltar</button></div><div class="card pad" style="max-width:560px;margin:8vh auto"><div class="eyebrow">ENTRAR</div><h1 class="title" style="font-size:2.8rem">Entre na batalha.</h1><p class="subtitle">Peça o código de 6 caracteres ao anfitrião.</p><div class="grid" style="margin-top:25px"><div class="field"><label for="code">Código da sala</label><input id="code" maxlength="6" placeholder="G7K2P9" style="text-transform:uppercase;letter-spacing:.18em;font-weight:800"></div><div class="field"><label for="name">Nome</label><input id="name" maxlength="18" placeholder="Como você quer aparecer?"></div><button class="btn" id="join-room">ENTRAR</button></div></div></div>`}
export function bindJoin(){document.querySelector('#code').oninput=e=>e.target.value=e.target.value.toUpperCase().replace(/[^A-HJ-NP-Z2-9]/g,'');document.querySelector('#join-room').onclick=async()=>{const roomCode=document.querySelector('#code').value.trim(),name=document.querySelector('#name').value.trim();if(roomCode.length!==6)return toast('O código deve ter 6 caracteres.','error');if(!name)return toast('Digite seu nome.','error');try{const data=await API.joinRoom({roomCode,name});appState.set({session:data.session,room:data.room,settings:data.room.settings});storage.set(data.session);router.navigate('lobby')}catch(e){toast(e.message,'error')}}}
